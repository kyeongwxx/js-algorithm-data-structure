// push - O(1)
// pop - O(1)
// shift - O(n) / need re-index
// unshift - O(n) / need re-index
// concat - O(n)
// slice - O(n)
// splice - O(n)
// sort - O(n * log n) / 정렬은 엘리먼트 당 한 번의 작업으로 끝나지 않을 수 있음
// forEach, map, filter, reduce etc - O(n)